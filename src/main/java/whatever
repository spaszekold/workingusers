todo
komentarze pamietajace autora
css into sass
no komenting unless logged in
vote system on comment
user profile

---

redactor panel
admin panel

---

 <div class='pagination pagination-centered'>
                <div class="row">
                    <div class="col-md-12 text-center">
                        <div class='pagination pagination-centered'>
                            <ul class="_pagination">
                                <li th:class="${currentIndex == 1}? 'disabled' : ''">
                                    <span th:if='${currentIndex == 1}'>← First</span>
                                    <a th:if='${currentIndex != 1}'
                                       th:href="@{${url + '/1'}}">← First</a>
                                </li>
                                <li th:class="${currentIndex != 1}? '' : 'disabled'">
                                    <span th:if='${currentIndex == 1}'>«</span>
                                    <a th:if='${currentIndex != 1}'
                                       th:href="@{${url + '/'+ (currentIndex - 1)}}"
                                       title='Go to previous page'>«</a>
                                </li>

                                <li th:each="item : ${#numbers.sequence(beginIndex,endIndex)}"
                                    th:class="${item == currentIndex ? 'active' : '' }">

                                    <span th:if='${item == currentIndex}' th:text='${item}'>1</span>

                                    <a th:if='${item != currentIndex}'
                                       th:href="@{${url + '/'+item }}">
                                        <span th:text='${item}'>1</span>
                                    </a>
                                </li>

                                <li th:class="${currentIndex != totalPages}? '' : 'disabled'">
                                    <span th:if='${currentIndex == totalPages}'>»</span>
                                    <a th:if='${currentIndex != totalPages}'
                                       th:href="@{${url + '/'+(currentIndex + 1)}}" title='Go to next page'>»</a>
                                </li>
                                <li th:class="${currentIndex == totalPages}? 'disabled' : ''">
                                    <span th:if='${currentIndex == totalPages}'>Last →</span>
                                    <a th:if='${currentIndex != totalPages}'
                                       th:href="@{${url + '/'+ totalPages}}">Last →</a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>






create table USERS (
  commentid BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  username varchar(256),
  password varchar(256),
  enabled boolean
);

create table AUTHORITIES (
  username varchar(256),
  authority varchar(256)
);

create TABLE posts (
  commentid BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  lilname varchar(200),
  fullname varchar(200),
  content varchar(10000),
  created TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

create table TAGS (
  commentid BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  tagname varchar(20)
);

create TABLE COMMENTS (
  commentid BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  postid BIGINT,
  lilname varchar(100),
  content varchar(250),
  created TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

create TABLE POSTS_TAGS (
  postid BIGINT,
  tagid BIGINT,
  PRIMARY KEY (postid, tagid),
  FOREIGN KEY (postid) REFERENCES posts(commentid),
  FOREIGN KEY (tagid) REFERENCES TAGS(commentid)

);